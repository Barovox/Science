import { Container } from "@mantine/core";
import { PublishProfiles } from "@/components";

const ProfilesPage = () => {
  //

  return (
    <Container fluid my="xl">
      <PublishProfiles.PublishProfiles />
    </Container>
  );
};

export default ProfilesPage;
